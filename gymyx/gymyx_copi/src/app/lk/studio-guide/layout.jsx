export const metadata = {
  title: 'Гид по студии | GYMYX',
  description: 'Гид по студии | GYMYX',
  manifest: '/manifest.json',
};

const AccountStydioGuide = ({ children }) => {
  return <div>{children}</div>;
};

export default AccountStydioGuide;